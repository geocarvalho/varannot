requests>=2.28
jinja2>=3.1

# Optional: only needed for the web UI (python -m varannot.web).
flask>=3.0

# Optional: only needed for AlphaGenome predictions
# (--alphagenome-key in the CLI / web, or python -m varannot.alphagenome_report).
# alphagenome
# matplotlib
